# Comac Claw 项目概要

> 新会话开始时请优先阅读本文件，再阅读对应功能 PRD 和现有源码。本文记录长期有效的项目边界与已确认决策，避免把演示实现误当成正式产品定义。

## 当前版本规划与 PRD 工作方式（2026-09-17，优先阅读）

- 正式 PRD 开始按真实上线产品重新梳理；已有 Demo 和历史 PRD 是讨论输入，不代表全部需求已经想清楚或进入本次交付。
- 用户当前倾向：2026 年 9 月版本聚焦 A2A、个人数字分身配置两个模块；具体子功能与最终上线范围待逐项确认，不视为整模块全量承诺。
- 组织后台本次不考虑；组织流程如何定义、系统智能等尚未想清楚，留待后续讨论，不为了沿用 Demo 而提前固化方案。
- 最新顺序调整：先编写个人数字分身配置正式 PRD，A2A 随后展开。用户明确九月不含岗位智能体模块；不能保留其能力继承、挂载评测、岗位版本与岗位优化等关联内容作为本版需求。
- 正式 PRD 采用产品视角，不展开技术方案；需要 Mermaid 交互流程。逐项说明原型可见文字是固定文案、业务值、生成内容、计算值还是状态文案，并定义来源、状态条件、操作后果和异常。缺少依据的事项留待讨论，讨论后再同步原型和文档。
- 当前评审稿：`docs/PRD-个人数字分身配置-2026年9月版.md`（V0.2，2026-09-17）。已梳理现状、40 项差异、14 项待决策和验收场景；建议方案不等于用户确认；仅按本轮后续授权补充双账号与新用户演示。随产品迭代持续维护需求、已确认决策、待确认项和版本范围。
- 下文历史“一期”“本期”“冻结口径”指对应阶段，不自动构成 9 月上线承诺；历史方案与本节冲突时，以本节及用户后续确认优先。
- 后台不纳入本次产品范围，不等于取消 A2A 必需的身份、人员查询、权限和消息等服务依赖；具体来源与最小接入要求在正式 PRD 中明确，不默认增加管理界面。

### 最新演示与写作反馈（2026-09-17）

- PRD 不重复定义公司已有通用交互规范，删除基础加载/保存状态大表和通用保存流程图；只展开业务特有的规则与流程。
- 已增加助理左下角双账号个人分身演示：陈智超（保留原有配置）、林晓（从空白配置开始）。切换进入个人分身页；两人画像、目标、能力、知识和成长分开保存，切换不会重置。
- 新用户不使用岗位智能体、知识地图示例和演示评测分数。原有陈智超演示保留，未批量应用其余待评审 PRD 改动。
- 账号切换仅为个人配置演示入口，不是正式认证；A2A、自动化与助理主会话的其他样例仍不在本次隔离范围。

### 记忆差异与待梳理项

- 当前个人配置的“用户心智”仅工作画像和目标；旧文档六卡结构不是当前原型。岗位说明、能力地图等不自动恢复。固定总经理身份、默认已应用画像、示例知识地图、预置成长事件及按字数/资源数量计算的评测分数均不能直接作为正式业务事实。
- 旧记忆中的助理侧栏四组会话列表已不符合当前入口：现有 `AssistantModeSidebar.tsx` 和《PRD-助理A2A群聊》采用“我的助理 / 定时器 / A2A 任务”，从 A2A 任务看板进入会话。四种通知/协作模式是业务分类，不等同于四组侧栏导航。最终正式信息架构随本轮 PRD 确认。
- 《PRD-助理A2A群聊》内部仍保留“左侧对应会话红点”等旧表述，后续编写正式稿时统一待确认提醒的位置与规则。
- A2A 九月范围需区分：通知与目标协作、任务派发与催办、C 项目平台联动、自动催办；不能因 Demo 已有而全部默认纳入。
- 业务任务状态、用户决策状态、消息投递状态需要分别定义；派发成功不等于业务任务完成。
- 本地协作任务是否需要接收确认、分身自动回复与人工确认的边界、多人协作长期等待时的处理，仍需在正式需求讨论中明确。


## 1. 项目定位

Comac Claw 是一个面向商飞业务场景的智能体应用前端原型，产品形态参考 WorkBuddy、Codex Desktop 等桌面智能体应用。

当前工程主要用于：

- 演示产品界面和完整交互路径；
- 验证任务、智能体和自动化等核心功能的产品设计；
- 为后续正式开发提供高保真原型；
- 配套产出面向真实开发的 PRD。

## 2. 必须遵守的双层口径

### 2.1 正式 PRD

PRD 描述真实产品最终应该如何工作，面向产品、前端、后端和测试人员。

- 业务规则、状态流转、调度逻辑、异常和验收标准必须真实；
- 不得因为当前 Demo 没有后端，就把正式需求写成浏览器本地模拟；
- 未确认的产品决策应明确标记为“待确认”，不能伪装成既定事实。

### 2.2 前端 Demo

Demo 是纯前端演示，不接真实后端和模型服务。

- 可以使用 Mock 数据、浏览器本地存储和模拟执行状态；
- 应尽量覆盖真实产品的页面、操作和状态，方便未来替换为真实接口；
- 模拟逻辑要集中管理，不要散落在展示组件中；
- Demo 限制只能写入原型说明或本文件，不能污染正式 PRD。

## 3. 当前技术方案

- React 19
- TypeScript
- Vite
- Lucide React 图标
- npm 启动

项目目录：`/Users/shinbouei/Documents/ComacAI/Comac-shuzifenshen`

启动方式：

```bash
npm install
npm run dev
```

## 4. 当前界面状态

已完成第一版智能体工作台：

- 左侧任务模式导航：新建任务、专家、自动化；助理改为右上角全局模式入口；
- 点击右上角助理后，左侧和主区域一起切换为助理工作区；助理侧栏包含“我的助理”、自动化任务、专家广场、技能广场、MCP 广场、A2A 单聊/群聊列表，底部保留“训练数字分身”和“后台管理”；
- 最近任务和自动化任务分组；
- 主工作区欢迎状态；
- 底部智能体输入器；
- 侧边栏收起与恢复；
- 任务点击和输入发送的 Mock 交互；
- 桌面端和移动端基础适配；
- 全局视觉倍率采用响应式策略：宽屏 112%、中等窗口 108%、移动端 100%；支持 CSS `zoom`，并为不支持 `zoom` 的浏览器提供 `transform` 降级；
- 已按要求删除页面顶部模拟的最小化、最大化和关闭窗口栏。

已完成自动化 Demo 第一版：

- 点击左侧“自动化”切换到自动化主工作区；
- 定时任务与运行记录页签；
- 自动化任务搜索和运行状态筛选；
- 创建、编辑和删除任务；
- 周期、按间隔、单次三类执行计划；
- 暂停、恢复和立即执行；
- 模拟“排队中 → 执行中 → 成功”的执行过程；
- 预置业务剧本和自定义任务通用模拟结果；
- 运行记录与运行详情抽屉；
- 任务、运行记录保存在浏览器本地；
- 删除自动化任务时，同步删除该任务关联的运行记录；
- 运行记录页面不展示状态/触发方式/时间范围筛选。

当前源码主要位于：

- `src/App.tsx`
- `src/App.css`
- `src/index.css`
- `src/automation/AutomationWorkspace.tsx`
- `src/automation/mockAutomation.ts`
- `src/automation/automation.css`
- `src/app/appTypes.ts`
- `src/admin/AdminWorkspace.tsx`
- `src/admin/AdminSidebar.tsx`
- `src/admin/AdminOverview.tsx`
- `src/admin/mockAdminData.ts`
- `src/admin/admin.css`
- `src/digital-twin/DigitalTwinTrainingWorkspace.tsx`
- `src/digital-twin/digital-twin.css`

后台管理 Demo 采用独立应用模式，不嵌入助理内容页：

- 应用模式统一定义为 `task | assistant | admin`；
- 从助理底部“后台管理”进入独立后台壳层，返回时恢复进入前的模式、侧边栏、助理目的地、群聊和 Workspace 状态；
- 后台导航、概览统计、占位管理页和 Mock 数据位于 `src/admin/`，后续新增管理模块不再扩展 `App.tsx` 的页面细节；
- 概览页和其他管理页均为纯前端 Mock，不连接真实管理接口、审批服务或权限服务。

已完成输出物预览 / Workspace 工作台 Demo 第三版：

- 仅在聊天对话中演示输出物，不接入自动化运行结果；
- 支持 HTML、PPT、Word、Markdown 四类输出物；
- 用户在输入框输入 `生成HTML`、`生成PPT`、`生成WORD`、`生成Markdown` 会触发对应前端剧本；
- 助手消息中展示输出物卡片，包含文件名、类型和大小；
- 生成输出物后不自动打开 Workspace；点击 HTML 卡片默认打开受控 Browser，点击其他卡片打开文件标签；
- 右侧工作区支持拖拽调整宽度；
- 普通任务和助理共用 `WorkspaceWorkbench` 组件与 `useWorkspaceWorkbench` 状态机，统一标签、输出物浮层、右侧文件导航、内容画布和响应式规则；
- 普通任务 Workspace 根目录为当前 Session，展示 `context.md`、`attachments/` 和 `outputs/`；
- 顶部固定保留“输出物”和“右侧文件导航”两个按钮，右侧导航内部不再放重复关闭按钮；
- 支持工作视图标签、`+` 启动器、标签去重、相邻标签选择和关闭最后标签自动退出 Workspace；
- Workspace 采用 Codex 式双层导航：一级顶栏管理标签、输出物与整个工作台，二级上下文栏管理路径、文件操作与文件目录；
- 一级 Workspace 按钮只负责隐藏/恢复整个工作台，隐藏时保留标签、激活标签和文件目录状态；二级文件夹按钮只切换目录，不再随状态改变语义；
- 关闭最后标签会真正清空工作现场并退出 Workspace；无标签时重新打开只展示启动器，不自动创建 `context.md` 或 `MEMORY.md`；
- 文件名不再以独立标题行重复展示，统一收敛到一级标签与二级路径；
- Workspace 专项 PRD V1.2 已补充组件控制权、核心状态机、关闭决策和输出物打开时序四张 Mermaid 图，后续开发以图和状态表共同为准；
- 输出物浮层只展示当前范围生成物，一期不展示来源；
- HTML Browser 地址只读，只允许运行自己生成的 HTML，支持刷新和查看源文件标签；
- 已移除旧版模块菜单、变更、来源、终端、底部面板和侧边任务；
- HTML、PPT、Word、Markdown 都在统一文件头展示文件名、元数据、路径和下载按钮；
- 右侧全局顶部不放分享、上传等操作，当前只保留具体输出物下载；
- 四类输出物使用静态内容分别模拟页面、幻灯片、Word 文档和 Markdown 文档预览；
- 该能力为纯前端 Demo，正式产品需通过服务端输出物存储、预览转换和下载接口实现。

已完成助理 A2A、群聊与统一 Workspace Demo：

- 点击页面右上角“助理”进入唯一、固定的个人数字分身逻辑长 Session；点击“返回任务”恢复原任务模式；
- 助理主会话不展示固定“今日关注”摘要条，协作事项由用户通过自然语言查询；
- 对话中可通过“我有哪些待办”等自然语言查询协作任务，并展示简洁结构化结果；
- 已删除独立“待我处理 / 我发起的”协作事项面板；
- “我的助理”右侧只保留统一 Workspace；进入助理模式时 Workspace 默认收起，展开后定位 Assistant 专属目录；A2A 会话页右侧改为当前会话的私人分身对话，不展示 Workspace 入口；
- 助理与普通任务使用同一 Workspace 组件和交互骨架，只切换根目录与内容；
- Assistant 目录演示 `SOUL.md`、`AGENT.md`、`USER.md`、`MEMORY.md`、每日工作记忆、协作任务投影和助理输出物；
- 点击对话中的任务查询结果，在 Workspace 中打开对应任务文件和事件记录；
- A2A 正式动作采用对话内结构化行动卡，不再使用底部 Dock、居中 Modal 或 Workspace 右下角浮层；
- 行动卡不覆盖输入器，待确认期间用户仍可继续聊天；
- Demo 不展示剧本按钮或专用演示入口，派任务、模型输出时收到催办和连续澄清均由自然语言触发；
- 派任务一期只允许一个主执行人，负责人和全部任务字段在同一张行动卡中编辑，不再先选人再进入第二步；
- 派发 Demo 固定展示名称、负责人、计划开始时间、计划完成时间和预估工时五个字段；正式产品仍由动态 Schema 决定字段；
- C 项目任务与 A2A 本地协作任务复用同一张卡；C 任务只显示“C项目管理任务”，本地协作不显示技术范围标记；
- 催办卡只展示且允许编辑任务名称、催办对象和催办内容，催办事件不写入或修改 C 项目管理平台；
- 顶部提供 Demo 专用的发起方/接收方视角切换；本期 C 项目任务由接收方被动接收，卡片只提供独立补充回复，不提供暂不处理、确认收到或申请调整；
- 所有 A2A 确认和连续澄清复用同一个配置驱动 `ActionCard`；场景仅提供步骤、控件、结构化字段和受控动作，不单独实现不同外壳；
- 同一工作流允许使用 `1/3` 等步骤完成连续澄清；点击选项只更新选中态，非末步点击“继续”才前进，末步点击“提交”才形成方案；
- 澄清只形成方案，不等于授权派任务或其他外部动作；派任务、催办、进展回复等最终按钮由受控动作类型映射为“确认派发 / 确认发送 / 确认回复”等文案；
- 行动卡字段必须来自 DecisionRequest、协作任务服务、组织身份服务或服务端 `SchemaSnapshot`，不允许前端从文案反向解析，也不允许模型自由生成正式动作按钮；
- 派发 Demo 在数据层保存 Mock 的 C 项目管理 Schema 版本，但界面不展示 Schema、版本、记录模式或执行链路；正式产品由后台动态配置字段、必填性、枚举和约束；
- 模型流式输出期间收到的决策先等待，输出结束后作为完整行动卡进入消息流；
- 用户可用自然语言修改待确认草稿；修改生成新版本，旧卡标记为已替代；
- 用户说“确认下发”不会正式执行，只有点击当前有效卡片中的受控确认按钮才获得授权；
- 不同事项的待确认卡可以同时存在并独立处理，不强制 FIFO；
- 出站行动卡不显示“取消草稿”按钮，草稿取消通过自然语言表达；需要用户决策的收到事项可以暂不处理，但 C 项目任务接收卡不属于待确认事项；不使用全局 `Esc` 取消历史行动卡；
- 支持模拟回复进展、发起催办确认和自然语言创建自动催办；
- 自动催办会进入现有自动化任务列表，可继续使用暂停、恢复、编辑、立即执行和删除；
- 当前目录、文件、任务和记忆全部为前端 Mock，不创建真实磁盘目录，不连接 Session、Workspace、Memory、项目平台、组织权限、MCP 或 A2A 服务。

已完成助理 SubAgent 协同执行过程 Demo 第一版：

- 主 Agent 识别复杂任务后，在输入器上方展示主 Agent 与动态 SubAgent 胶囊；
- 点击 SubAgent 胶囊可在助理对话区域内查看像普通模型回复一样逐段出现的执行文字，不使用时间线、步骤卡或独立产出卡；
- 详情层保留胶囊栏，可在不同 SubAgent 间直接切换，点击主 Agent 或底部按钮返回主会话；
- 当前轮运行期间输入器整体阻塞，完成后恢复；
- 胶囊在本轮完成后保留，用户成功发送下一条消息时清理；
- 支持部分失败、自然语言失败说明和主会话整轮重新执行；主 Agent完成后直接输出正常回复，不展示额外“全部完成”状态卡；
- Demo 通过自然语言触发固定 Mock 剧本，正式产品由主 Agent 调度和服务端事件流驱动；
- 正式产品基线见 `docs/PRD-助理SubAgent协同执行过程.md`。

已完成岗位智能体前后台 Demo：

- 当前产品基线为“一个个人数字分身可主动添加多个岗位智能体，并分别启用、停用或移除”；能力按资源 ID 合并去重，个人知识仍独立维护；
- 个人配置中的“工作画像”采用“让助理更懂我”引导：基础了解固定为工作影响范围（单选）和关注对象（最多三项）两题，随后模拟追问、可跳过的预置材料、直接生成结果；结果页隐藏步骤栏，区分待确认和已应用，支持返回修改与更新画像。确认后持久化答案和画像并形成成长记录，取消更新保留旧画像，兼容旧版 Markdown；Demo 不上传真实材料、不调用模型；
- 前台岗位详情按岗位目标、核心职责、关键交付物、工作边界及人工确认、可用能力展示，移除旧数据中的上下游协作展示；名称和版本沿用已发布岗位，不新增前后台完整联动；
- 已实现岗位创建与配置、能力地图、评测发布、独立审批、个人添加/启停、运行治理和受控进化等前端 Mock 闭环；
- 当前产品与实现依据按优先级阅读：`docs/岗位智能体前后台方案/README.md` → 总体方案/前后台 PRD → `后台配置改版-能力地图.md` → `实现与验收.md`；
- 全部数据、身份、权限、组织、模型与业务连接仍为前端 Mock，正式接入边界见“实现与验收”。

## 5. 自动化功能已确认范围

点击左侧“自动化”后，主工作区切换为自动化管理页。

本期页面包括：

- 定时任务列表；
- 运行记录；
- 新建和编辑自动化任务；
- 搜索；
- 暂停、恢复、立即执行、删除；
- 任务状态、执行频率、下次执行时间；
- 单次运行状态和结果查看；
- 任务列表只展示“当前”和“已暂停”两组；
- 定时任务列表右侧不展示成功/失败、排队中、执行中等运行状态，运行状态只在运行记录中展示；
- 运行记录列表任务名称下方只显示 `手动触发/自动触发/重新执行`；时间显示在行最右侧。
- 左侧全局侧边栏“自动化任务”区由运行记录动态生成：新建未运行不创建文件夹，文件夹不可删除，文件夹内运行会话可删除，删空后文件夹隐藏，全部删空后整个自动化任务区隐藏。
- 创建/编辑定时任务表单不显示“专家”选择，也不显示底部“执行计划预览”。

本期明确不考虑：

- 连接器；
- 推送到微信小程序；
- 生效日期区间。

参考截图位于上层目录：

- `../refernece/workbudy-定时任务.png`
- `../refernece/workbuddy-创建定时任务.png`

注意：目录名称当前为 `refernece`，不要擅自重命名，以免引用失效。

## 6. 自动化 Demo 的推荐实现

采用“预置剧本 + 通用模拟”的混合方式：

- 预置两三个适合演示的自动化任务和高质量执行结果；
- 用户可以创建任意任务，保存到浏览器本地；
- 支持编辑、删除、暂停、恢复和立即执行；
- 执行时模拟“排队中 → 执行中 → 成功/失败”；
- 每次执行生成一条运行记录；
- 左侧全局侧边栏只展示已经产生运行记录的自动化任务文件夹；
- 删除自动化任务会同步删除该任务运行记录；删除左侧运行会话只删除对应运行记录，不删除自动化任务配置；
- 新建任务的自定义提示词生成通用模拟结果，而不是简单原样回显；
- 浏览器关闭后不保证真实定时执行，这是 Demo 限制，不是正式产品规则。

正式需求见：`docs/PRD-自动化定时任务.md`。该文档已升级为 V1.0 开发基线，包含页面路由、字段校验、交互文案、任务与运行状态机、服务端调度、接口契约、错误码、竞态处理、安全、操作记录和验收用例。后续实现前应先阅读完整 PRD，不要仅根据参考截图猜测业务规则。

## 7. 输出物预览功能已确认范围

本期输出物预览只覆盖聊天场景。

已确认：

- 预览区是可拖拽宽度的右侧工作区，不是简单浮层；
- 只做聊天输出物；
- 只做预览，不做在线编辑；
- 支持 HTML、PPT、Word、Markdown；
- 对话区输出物以卡片展示，卡片包含名称、类型、大小；
- 顶部有输出物和右侧文件导航入口；普通任务默认定位当前 Session，助理默认定位 `Assistant/`；
- 需要下载功能，Demo 中先用前端按钮模拟；
- Demo 可以用固定剧本触发，真实 PRD 不能写成字符串匹配实现。

正式能力见：`docs/PRD-右侧工作区与输出物预览.md`；精确交互状态机见：`docs/PRD-Workspace工作台交互规范.md`。两者共同构成当前 Workspace 开发基线。

## 8. 后续工作建议

### 8.1 助理 A2A 已确认产品判断

- 任务型 A2A 继续覆盖任务派发和任务催办；新增 A2A 单聊/群聊，但仍不做 Agent 自由互相调用；
- A2A 会话由“交互范围（单聊/群聊）× 事项机制（通知型/目标协作型）”组成四种组合；
- A2A 新会话只能在“我的助理”长会话中通过自然语言创建，不提供加号或成员表单；已有会话通过页面右侧、按用户和会话隔离的私人分身对话发送和回复；
- A2A 中间正式记录区完全只读，主持人和参与人都不能直接输入，正式记录中只出现数字分身发言；
- 助理侧栏固定按多人通知、多人协作、单人协作、单人通知四组展示，不显示分组数量；
- 会话红点只表示当前用户存在未处理的 Ask User，不表示未读，也不显示数量；打开会话不会清除红点，完成确认后才清除；
- A2A 会话页已删除通知说明、目标/轮次进度摘要、私人上下文说明和只读提示条；不展示“已完成、进行中”等常驻状态标签，底层状态继续驱动顺序与 Ask User；
- 多人协作遇到 Ask User 时严格挂起当前顺序，主持人不能代答、跳过、强制继续或发起下一轮；
- 群聊协作中的各数字分身严格按名单顺序各回答一次，后发言分身可以读取本轮前序回复；通知型不强制回复；
- 私人助理长 Session 与群聊是独立执行上下文，群聊消息不进入助理 FIFO 通知队列；
- 用户始终只与自己的数字分身对话；
- A2A 是后台协作能力，不增加“A2A”一级导航；
- 对适用的业务任务，C 项目管理平台是事实源；明确不适用 C 平台的 `assistant_local` 协作由协作任务服务作为事实源；
- 一期不在 Comac Claw 内重复创建项目和邀请成员；
- 普通任务模式可以持续创建独立 Session，按创建日期归档；助理模式每个用户只有一个逻辑长 Session；
- 助理长会话承载全局自然语言交互，右侧保留统一 Workspace；A2A 会话是明确例外，右侧展示当前会话的私人分身上下文且不提供 Workspace；
- 协作任务查询结果在对话中展示，点击后在 `Assistant/collaboration/tasks/` 中打开任务文件投影；
- Assistant 目录包含助理规则、用户偏好、长期记忆、每日工作记忆、协作任务投影和助理输出物；
- 每日工作记忆不是新 Session；长 Session 底层仍需上下文压缩、记忆提炼和按需检索；
- Assistant Workspace 默认不展示全部普通任务，但助理可在用户授权下按需检索普通任务；
- 本期不引入 `Projects/` 一级目录；只有未来明确支持多 Session 共享上下文、成员和权限时再评估；
- 任务派发和催办属于对他人的外部动作，执行前必须由用户确认；
- 用户决策采用对话内结构化行动卡，不是 Workspace 文件组件、底部 Dock 或居中 Modal；
- 用户处于助理模式时，模型输出结束后行动卡进入消息流；输入器保持可用，用户可以继续聊天；
- 用户处于普通任务模式时不展示 A2A 决策，普通任务 Workspace 也不存在 `collaboration/`；只有存在待本人确认的 Ask User 时，“助理”入口才显示无数字红点，普通新消息不点亮；
- 不同事项可以同时处于待确认状态；同一事项的新草稿版本替代旧卡，旧版本按钮失效；
- 自然语言可以创建、补充和修订草稿，但只有受控确认按钮可以执行正式外部动作；
- 一期派任务只允许一个主执行人；
- 一期决策类型包括派发确认、催办确认和收到催办后的进展回复；C 项目任务收到卡为被动接收信息卡，只允许补充回复，不属于待确认决策；
- 发起方确认催办后，接收方助理显示轻量催办卡；卡片仅包含“{发起人}催办：{任务名称}”标题、补充回复输入框和小型发送按钮，回复不自动修改任务状态；
- A2A 消息只传协作事件或对象引用，不取代正式业务数据；
- A2A 任务文件是权威任务服务的只读投影，不是唯一事实源；
- 任务派发和催办统一生成 `collaboration_task_id`，并根据适用性分为两种记录模式：`c_project_bound` 写入 C 项目管理平台并保存外部任务映射，`assistant_local` 仅由协作任务服务保存；两者都生成 `Assistant/collaboration/` 文件投影；
- 模型只能建议记录模式，最终必须由服务端根据 C 项目关联、任务类型、字段完整性、用户权限和接口可用性判定；缺字段、无权限和平台临时故障不得静默降级为 `assistant_local`；
- C 项目管理平台临时不可用时标记 `pending_sync` 并受控重试；只有明确不属于 C 平台承载范围的协作才允许仅保存在助理协作域；
- 自动催办复用现有自动化模块；
- 双模式和目录总纲见 `docs/PRD-双模式会话与统一Workspace.md`；
- 业务流程基线见 `docs/PRD-助理A2A任务派发与催办.md`，用户决策交互基线见 `docs/PRD-A2A用户决策与确认机制.md`；
- 完整场景讲解见 `docs/A2A用户决策与确认机制-完整方案.html`。

### 8.2 新会话交接：A2A 当前冻结口径（2026-07-27）

新会话讨论 A2A 时，以下内容视为当前产品基线，除非用户明确要求推翻：

#### 发起方

- 派任务和催办都先在对话消息流内生成结构化卡片，只有点击卡片主按钮才执行正式外部动作；聊天中的“下发吧”“确认”不构成授权。
- 派任务只有一张表单卡，负责人和其他字段在同一张卡内；Demo 固定五个字段，正式产品字段来自服务端动态 Schema。
- `c_project_bound` 只显示“C项目管理任务”业务标记；`assistant_local` 不显示技术标记。
- 催办卡只有三个可编辑字段：任务名称、催办对象、催办内容。催办动作不创建或修改 C 项目管理任务。
- 出站卡不显示“取消草稿”；草稿取消或修改通过自然语言完成。
- 派发确认后的历史摘要右侧暂统一显示“已完成”。这里的“已完成”只表示派发动作执行完成，不代表接收方已经完成业务任务。

#### 接收方

- C 项目任务送达即被动接收，不进入 `pending_confirmation`，不提供暂不处理、确认收到、申请调整或拒绝。
- C 项目任务接收卡展示只读任务字段，底部只有“补充回复”输入框和小型发送按钮。
- 发起方确认催办后，接收方出现轻量 `reminder_receipt` 卡；卡片只有“{发起人}催办：{任务名称}”标题、补充回复输入框和发送按钮。
- 接收方补充回复只生成 A2A 回复消息和协作历史事件，不自动改变任务进度、完成状态或 C 项目管理平台数据。
- `assistant_local` 任务的显式确认收到能力在当前 Demo 暂保留；是否与 C 项目任务统一为被动接收尚未最终确认。

#### 用户可见信息与后台信息

- 卡片不展示 Schema ID/版本、草稿版本、记录模式、数据落点或“执行目标”等技术文案。
- `decision_version`、`draft_revision`、Schema、记录模式、授权和幂等信息必须继续在后台及审计数据中保存。
- 同一事项被自然语言修改时生成新卡；旧卡只显示“已由新草稿替代”，不向用户暴露版本号。

#### 当前 Demo 自然语言剧本

- “把发动机风险评审材料整理任务派给李静……”：生成 C 项目派发卡。
- “创建一个仅 A2A 任务，派给张三……”：生成 `assistant_local` 派发卡。
- “催办张三”：生成催办确认卡；确认后通过 A2A 回调送达接收方自己的助理，当前原型不提供跨身份视角切换。
- “组织一次专项评审”：进入多步澄清卡，非末步“继续”，末步“提交”。
- “帮我总结协作进展”：模拟模型输出期间收到 A2A 事项，等当前输出结束后再插入卡片。

#### 下一轮优先讨论：状态体系

- 需要拆分三类状态：业务任务状态、行动卡/决策状态、A2A 投递与阅读状态，不能混成一个枚举。
- 需要明确“已完成、已处理、已拒绝、已接收、已回复”分别描述哪个对象，以及谁有权触发。
- 当前建议原则：状态集合、转换条件和权威写入由产品规则与后端状态机确定；模型只负责把自然语言映射成候选动作或建议状态，不能自行决定权威状态。
- 需要特别避免把派发摘要的“已完成”误解为底层任务已经完成。
- 后期若增加拒绝、申请调整、显式收悉，必须同时定义权限、C 项目管理平台写回、双方通知和 Workspace 投影规则。

### 8.3 后续工作建议

1. 由用户评审对话内行动卡的信息密度、版本替代、自然语言修订和 Workspace 联动；
2. 确认 C 项目管理平台动态 Schema 接口、字段类型白名单和组织权限来源；
3. 优先定义业务任务、行动卡决策、A2A 投递三层状态机及其权威来源；
4. 明确“强制任务派发”和“可拒绝协作请求”是否需要拆分；
5. 根据真实接口补充加载、空状态、权限失败、消息投递失败和并发冲突；
6. 继续完善自动化失败、终止、重新执行等次级演示路径；
7. 根据评审结果更新正式 PRD，但始终保持真实产品口径。

### 8.4 新会话接续：当前界面与工程冻结口径（2026-08-31）

新会话继续开发前，优先按以下顺序获取上下文：

1. 阅读本文件，确认正式产品口径与前端 Demo 口径；
2. 涉及主持人制群聊时阅读 `docs/PRD-助理A2A群聊.md`；
3. 涉及任务派发、催办和行动卡时阅读对应 A2A PRD；
4. 涉及实现时再查看 `src/App.tsx`、`src/assistant/`、`src/admin/` 和 `src/workspace/`，不得只根据旧截图推断当前行为。

当前界面决策：

- 任务模式保留原任务侧栏；“助理”是页面右上角的全局模式入口，不再占用任务侧栏一级导航；
- 助理模式替换整套左侧栏和主内容，群聊与私人助理共用助理模式，但保持独立执行上下文；
- 新建单人、多人会话只能通过“我的助理”中的自然语言发起；进入任一会话后，右侧显示当前用户与自己分身的私人对话，中间正式记录区不提供输入框；
- A2A 会话形成单聊通知、单聊协作、群聊通知、群聊协作四种组合；通知型不跟踪回复，协作型跟踪目标、轮次和预期回复；
- 群聊协作中每位数字分身严格按名单顺序发言一次，后发言分身可以读取本轮前序回答；
- 助理侧栏按多人通知、多人协作、单人协作、单人通知分成四组；红点只代表待本人确认，不代表未读且不显示数量；
- Ask User 出现在需要确认者自己的右侧私人分身对话中，触发后当前顺序挂起；主持人只能等待，不能跳过或继续；
- 助理忙碌时，新建群指令排队且不打断当前输出；已经存在的群聊仍可独立使用；
- “我的助理”Workspace 初始收起；展开时“返回任务”使用顶栏预留区域，不覆盖 Workspace 控件；A2A 会话页不展示 Workspace 入口；
- “后台管理”是第三种独立应用模式，不是助理内容区中的普通占位页；左上角返回后恢复进入后台前的助理、群聊、侧边栏和 Workspace 现场；
- 后台概览、专家、技能、MCP、模型、自动化、审批、组织、角色和分类目前全部是前端 Mock，后续从 `src/admin/` 内逐页扩展。

当前工程约束：

- 应用模式统一使用 `task | assistant | admin`，定义位于 `src/app/appTypes.ts`；
- 后台页面、导航、图表和 Mock 数据保持在 `src/admin/` 内，不继续把页面细节堆入 `src/App.tsx`；
- `App.tsx` 只负责跨模式共享状态和切换；新增业务模块优先放入独立目录；
- 暂不引入 Redux、复杂前端路由或后端领域层；只有 Demo 状态明显跨越多个模块时再评估；
- 进入后台时原任务/助理界面只隐藏不销毁，这是返回后保留现场的基础，不得无意改成重新挂载。

## 后台岗位配置更新（2026-09-08）

- 新建先以组织树单选岗位，确认创建草稿并进入与已有草稿共用的配置工作台；左侧树按持久化岗位 ID 定位。
- 右侧分为能力地图、岗位画像、能力配置和版本记录。能力地图由集中本体示例模型展示多流程、节点输入输出及条件回退；尚未接入真实 C 大脑服务。
- 输出模板、评测用例配置入口与对应必填门槛已移除；岗位能力校验与人工复核仍保留。
- 详细增量基线：`docs/岗位智能体前后台方案/后台配置改版-能力地图.md`。
