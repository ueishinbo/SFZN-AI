# COMAC AI 项目概要

> 新会话开始时请优先阅读本文件，再阅读对应功能 PRD 和现有源码。本文记录长期有效的项目边界与已确认决策，避免把演示实现误当成正式产品定义。

## 1. 项目定位

COMAC AI 是一个面向商飞业务场景的智能体应用前端原型，产品形态参考 WorkBuddy、Codex Desktop 等桌面智能体应用。

当前工程主要用于：

- 演示产品界面和完整交互路径；
- 验证任务、智能体和自动化等核心功能的产品设计；
- 为后续正式开发提供高保真原型；
- 配套产出面向真实开发的 PRD。

## 2. 必须遵守的双层口径

### 2.1 正式 PRD

PRD 描述真实产品最终应该如何工作，面向产品、前端、后端和测试人员。

- 业务规则、状态流转、调度逻辑、异常和验收标准必须真实；
- 不得因为当前 Demo 没有后端，就把正式需求写成浏览器本地模拟；
- 未确认的产品决策应明确标记为“待确认”，不能伪装成既定事实。

### 2.2 前端 Demo

Demo 是纯前端演示，不接真实后端和模型服务。

- 可以使用 Mock 数据、浏览器本地存储和模拟执行状态；
- 应尽量覆盖真实产品的页面、操作和状态，方便未来替换为真实接口；
- 模拟逻辑要集中管理，不要散落在展示组件中；
- Demo 限制只能写入原型说明或本文件，不能污染正式 PRD。

## 3. 当前技术方案

- React 19
- TypeScript
- Vite
- Lucide React 图标
- npm 启动

项目目录：`/Users/shinbouei/Documents/ComacAI/Comac-shuzifenshen`

启动方式：

```bash
npm install
npm run dev
```

## 4. 当前界面状态

已完成第一版智能体工作台：

- 左侧主导航：新建任务、助理、专家、自动化；
- 最近任务和自动化任务分组；
- 主工作区欢迎状态；
- 底部智能体输入器；
- 侧边栏收起与恢复；
- 任务点击和输入发送的 Mock 交互；
- 桌面端和移动端基础适配；
- 全局视觉倍率采用响应式策略：宽屏 112%、中等窗口 108%、移动端 100%；支持 CSS `zoom`，并为不支持 `zoom` 的浏览器提供 `transform` 降级；
- 已按要求删除页面顶部模拟的最小化、最大化和关闭窗口栏。

已完成自动化 Demo 第一版：

- 点击左侧“自动化”切换到自动化主工作区；
- 定时任务与运行记录页签；
- 自动化任务搜索和运行状态筛选；
- 创建、编辑和删除任务；
- 周期、按间隔、单次三类执行计划；
- 暂停、恢复和立即执行；
- 模拟“排队中 → 执行中 → 成功”的执行过程；
- 预置业务剧本和自定义任务通用模拟结果；
- 运行记录与运行详情抽屉；
- 任务、运行记录保存在浏览器本地；
- 删除自动化任务时，同步删除该任务关联的运行记录；
- 运行记录页面不展示状态/触发方式/时间范围筛选。

当前源码主要位于：

- `src/App.tsx`
- `src/App.css`
- `src/index.css`
- `src/automation/AutomationWorkspace.tsx`
- `src/automation/mockAutomation.ts`
- `src/automation/automation.css`

已完成输出物预览 / Workspace 工作台 Demo 第三版：

- 仅在聊天对话中演示输出物，不接入自动化运行结果；
- 支持 HTML、PPT、Word、Markdown 四类输出物；
- 用户在输入框输入 `生成HTML`、`生成PPT`、`生成WORD`、`生成Markdown` 会触发对应前端剧本；
- 助手消息中展示输出物卡片，包含文件名、类型和大小；
- 生成输出物后不自动打开 Workspace；点击 HTML 卡片默认打开受控 Browser，点击其他卡片打开文件标签；
- 右侧工作区支持拖拽调整宽度；
- 普通任务和助理共用 `WorkspaceWorkbench` 组件与 `useWorkspaceWorkbench` 状态机，统一标签、输出物浮层、右侧文件导航、内容画布和响应式规则；
- 普通任务 Workspace 根目录为当前 Session，展示 `context.md`、`attachments/` 和 `outputs/`；
- 顶部固定保留“输出物”和“右侧文件导航”两个按钮，右侧导航内部不再放重复关闭按钮；
- 支持工作视图标签、`+` 启动器、标签去重、相邻标签选择和关闭最后标签自动退出 Workspace；
- Workspace 采用 Codex 式双层导航：一级顶栏管理标签、输出物与整个工作台，二级上下文栏管理路径、文件操作与文件目录；
- 一级 Workspace 按钮只负责隐藏/恢复整个工作台，隐藏时保留标签、激活标签和文件目录状态；二级文件夹按钮只切换目录，不再随状态改变语义；
- 关闭最后标签会真正清空工作现场并退出 Workspace；无标签时重新打开只展示启动器，不自动创建 `context.md` 或 `MEMORY.md`；
- 文件名不再以独立标题行重复展示，统一收敛到一级标签与二级路径；
- Workspace 专项 PRD V1.2 已补充组件控制权、核心状态机、关闭决策和输出物打开时序四张 Mermaid 图，后续开发以图和状态表共同为准；
- 输出物浮层只展示当前范围生成物，一期不展示来源；
- HTML Browser 地址只读，只允许运行自己生成的 HTML，支持刷新和查看源文件标签；
- 已移除旧版模块菜单、变更、来源、终端、底部面板和侧边任务；
- HTML、PPT、Word、Markdown 都在统一文件头展示文件名、元数据、路径和下载按钮；
- 右侧全局顶部不放分享、上传等操作，当前只保留具体输出物下载；
- 四类输出物使用静态内容分别模拟页面、幻灯片、Word 文档和 Markdown 文档预览；
- 该能力为纯前端 Demo，正式产品需通过服务端输出物存储、预览转换和下载接口实现。

已完成助理 A2A 与统一 Workspace Demo 第三版：

- 点击左侧“助理”进入唯一、固定的个人数字分身逻辑长 Session；
- 助理主会话不展示固定“今日关注”摘要条，协作事项由用户通过自然语言查询；
- 对话中可通过“我有哪些待办”等自然语言查询协作任务，并展示简洁结构化结果；
- 已删除独立“待我处理 / 我发起的”协作事项面板；
- 右侧只保留统一 Workspace，默认展示 Assistant 专属目录；
- 助理与普通任务使用同一 Workspace 组件和交互骨架，只切换根目录与内容；
- Assistant 目录演示 `SOUL.md`、`AGENT.md`、`USER.md`、`MEMORY.md`、每日工作记忆、协作任务投影和助理输出物；
- 点击对话中的任务查询结果，在 Workspace 中打开对应任务文件和事件记录；
- A2A 正式动作不再使用对话内确认卡或居中 Modal；只要聊天区可见，决策 Dock 占用输入器原位置并向上展开，宽屏双栏时不得移动到 Workspace 右下角；
- 中窄屏 Workspace 独占主区域时，决策 Dock 固定在整个主区域底部；
- Demo 不展示剧本按钮或专用演示入口，派任务、模型输出时收到催办和连续澄清均由自然语言触发；
- 决策 Dock 不使用全屏遮罩或背景模糊；输入器暂时被覆盖，但原草稿和输入状态完整保留；
- 派任务一期只允许一个主执行人，面板内完成组织身份选择、任务核对和确认派发；
- 选人默认展示三名助理推荐人员，用户点击“选择其他成员”后才展开组织搜索；
- 收到催办面板不单独展示“截止时间 / 最近进展”摘要块，完整上下文保留在 Workspace；
- 所有 A2A 确认和连续澄清复用同一个配置驱动 `DecisionDock`；场景仅提供步骤、控件、结构化字段和受控动作，不单独实现不同外壳；
- 同一工作流允许使用 `1/3` 等步骤完成连续澄清；点击选项只更新选中态，非末步点击“继续”才前进，末步点击“提交”才形成方案；
- 澄清只形成方案，不等于授权派任务或其他外部动作；派任务、催办、进展回复等最终按钮由受控动作类型映射为“确认派发 / 确认发送 / 确认回复”等文案；
- 决策面板字段必须来自 DecisionRequest、协作任务服务、组织身份服务或经校验的 Agent/Skill Schema，不允许前端从自然语言文案中反向解析，也不允许模型自由生成正式动作按钮；
- 模型流式输出期间收到的决策先进入内部等待，输出结束后再弹出；
- 除模型正在输出外，输入草稿、聊天浏览和 Workspace 查看都会被立即打断，但底层现场完整保留；
- “稍后处理”和 `Esc` 表示暂存，不是拒绝；不提供右上角 `X`，任务文件写入 `deferred_by_user`，后续可通过助理自然语言恢复；
- 多事项正式产品使用内部 FIFO，一次只展示一个，不提供上一项、下一项、页码或显式队列；
- 支持模拟回复进展、发起催办确认和自然语言创建自动催办；
- 自动催办会进入现有自动化任务列表，可继续使用暂停、恢复、编辑、立即执行和删除；
- 当前目录、文件、任务和记忆全部为前端 Mock，不创建真实磁盘目录，不连接 Session、Workspace、Memory、项目平台、组织权限、MCP 或 A2A 服务。

## 5. 自动化功能已确认范围

点击左侧“自动化”后，主工作区切换为自动化管理页。

本期页面包括：

- 定时任务列表；
- 运行记录；
- 新建和编辑自动化任务；
- 搜索；
- 暂停、恢复、立即执行、删除；
- 任务状态、执行频率、下次执行时间；
- 单次运行状态和结果查看；
- 任务列表只展示“当前”和“已暂停”两组；
- 定时任务列表右侧不展示成功/失败、排队中、执行中等运行状态，运行状态只在运行记录中展示；
- 运行记录列表任务名称下方只显示 `手动触发/自动触发/重新执行`；时间显示在行最右侧。
- 左侧全局侧边栏“自动化任务”区由运行记录动态生成：新建未运行不创建文件夹，文件夹不可删除，文件夹内运行会话可删除，删空后文件夹隐藏，全部删空后整个自动化任务区隐藏。
- 创建/编辑定时任务表单不显示“专家”选择，也不显示底部“执行计划预览”。

本期明确不考虑：

- 连接器；
- 推送到微信小程序；
- 生效日期区间。

参考截图位于上层目录：

- `../refernece/workbudy-定时任务.png`
- `../refernece/workbuddy-创建定时任务.png`

注意：目录名称当前为 `refernece`，不要擅自重命名，以免引用失效。

## 6. 自动化 Demo 的推荐实现

采用“预置剧本 + 通用模拟”的混合方式：

- 预置两三个适合演示的自动化任务和高质量执行结果；
- 用户可以创建任意任务，保存到浏览器本地；
- 支持编辑、删除、暂停、恢复和立即执行；
- 执行时模拟“排队中 → 执行中 → 成功/失败”；
- 每次执行生成一条运行记录；
- 左侧全局侧边栏只展示已经产生运行记录的自动化任务文件夹；
- 删除自动化任务会同步删除该任务运行记录；删除左侧运行会话只删除对应运行记录，不删除自动化任务配置；
- 新建任务的自定义提示词生成通用模拟结果，而不是简单原样回显；
- 浏览器关闭后不保证真实定时执行，这是 Demo 限制，不是正式产品规则。

正式需求见：`docs/PRD-自动化定时任务.md`。该文档已升级为 V1.0 开发基线，包含页面路由、字段校验、交互文案、任务与运行状态机、服务端调度、接口契约、错误码、竞态处理、安全、操作记录和验收用例。后续实现前应先阅读完整 PRD，不要仅根据参考截图猜测业务规则。

## 7. 输出物预览功能已确认范围

本期输出物预览只覆盖聊天场景。

已确认：

- 预览区是可拖拽宽度的右侧工作区，不是简单浮层；
- 只做聊天输出物；
- 只做预览，不做在线编辑；
- 支持 HTML、PPT、Word、Markdown；
- 对话区输出物以卡片展示，卡片包含名称、类型、大小；
- 顶部有输出物和右侧文件导航入口；普通任务默认定位当前 Session，助理默认定位 `Assistant/`；
- 需要下载功能，Demo 中先用前端按钮模拟；
- Demo 可以用固定剧本触发，真实 PRD 不能写成字符串匹配实现。

正式能力见：`docs/PRD-右侧工作区与输出物预览.md`；精确交互状态机见：`docs/PRD-Workspace工作台交互规范.md`。两者共同构成当前 Workspace 开发基线。旧版 `docs/PRD-输出物预览.md` 仅作为历史草案保留，不作为当前实现基线。

## 8. 后续工作建议

### 8.1 助理 A2A 已确认产品判断

- 一期只覆盖任务派发和任务催办，不做自由 Agent 群聊；
- 用户始终只与自己的数字分身对话；
- A2A 是后台协作能力，不增加“A2A”一级导航；
- 业务项目平台是项目、成员和任务的唯一事实源；
- 一期不在 COMAC AI 内重复创建项目和邀请成员；
- 普通任务模式可以持续创建独立 Session，按创建日期归档；助理模式每个用户只有一个逻辑长 Session；
- 助理长会话承载自然语言交互，右侧只保留统一 Workspace，不设置独立协作事项面板；
- 协作任务查询结果在对话中展示，点击后在 `Assistant/collaboration/tasks/` 中打开任务文件投影；
- Assistant 目录包含助理规则、用户偏好、长期记忆、每日工作记忆、协作任务投影和助理输出物；
- 每日工作记忆不是新 Session；长 Session 底层仍需上下文压缩、记忆提炼和按需检索；
- Assistant Workspace 默认不展示全部普通任务，但助理可在用户授权下按需检索普通任务；
- 本期不引入 `Projects/` 一级目录；只有未来明确支持多 Session 共享上下文、成员和权限时再评估；
- 任务派发和催办属于对他人的外部动作，执行前必须由用户确认；
- 用户决策不是对话内消息卡、Workspace 文件组件或居中 Modal；聊天区可见时占用输入器原位置并向上展开，中窄屏 Workspace 独占时固定在主区域底部；
- 用户处于助理模式时，除模型正在输出外，决策立即出现；模型输出结束后立即展示，不使用全屏遮罩或背景模糊；
- 用户处于普通任务模式时不展示 A2A 决策，普通任务 Workspace 也不存在 `collaboration/`；左侧“助理”入口以轻量圆点提示新事项；
- “稍后处理”或 `Esc` 只表示暂存，不发送拒绝或其他 A2A 回复；事项持续保存在协作服务和 Assistant Workspace 投影中；
- 多个同时到达的决策在内部按 FIFO 等待，一次只展示一个；界面不显示页码、前后切换或队列数量；
- 一期派任务只允许一个主执行人；
- 一期决策类型包括派发确认、催办确认、收到任务处理和收到催办后的进展回复；普通状态通知继续留在对话和文件中；
- A2A 消息只传协作事件或对象引用，不取代正式业务数据；
- A2A 任务文件是权威任务服务的只读投影，不是唯一事实源；
- 任务派发和催办统一生成 `collaboration_task_id`，并根据适用性分为两种记录模式：`c_project_bound` 写入 C 项目管理平台并保存外部任务映射，`assistant_local` 仅由协作任务服务保存；两者都生成 `Assistant/collaboration/` 文件投影；
- 模型只能建议记录模式，最终必须由服务端根据 C 项目关联、任务类型、字段完整性、用户权限和接口可用性判定；缺字段、无权限和平台临时故障不得静默降级为 `assistant_local`；
- C 项目管理平台临时不可用时标记 `pending_sync` 并受控重试；只有明确不属于 C 平台承载范围的协作才允许仅保存在助理协作域；
- 自动催办复用现有自动化模块；
- 双模式和目录总纲见 `docs/PRD-双模式会话与统一Workspace.md`；
- 业务流程基线见 `docs/PRD-助理A2A任务派发与催办.md`，用户决策交互基线见 `docs/PRD-A2A用户决策与确认机制.md`；
- 完整场景讲解见 `docs/A2A用户决策与确认机制-完整方案.html`。

### 8.2 后续工作建议

1. 由用户评审 A2A 决策 Dock 的信息密度、输入器依附关系、自然语言触发和 Workspace 联动；
2. 确认首个真实业务平台、任务字段和组织权限来源；
3. 明确“强制任务派发”和“可拒绝协作请求”是否需要拆分；
4. 根据真实接口补充加载、空状态、权限失败、消息投递失败和并发冲突；
5. 继续完善自动化失败、终止、重新执行等次级演示路径；
6. 根据评审结果更新正式 PRD，但始终保持真实产品口径。
